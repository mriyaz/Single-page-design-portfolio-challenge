export default interface IBoxComponent {
    bgColor: string,
    imageUrl: string,
    height: string,
    title: string
}

export interface IComponent {
    name:string, bgColor:string, image:string, span:string
}